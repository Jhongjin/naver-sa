# Supabase

This directory will contain Supabase schema migrations, seed files, and local Supabase configuration for Naver SA Autopilot.

## Planned Structure

```text
supabase/
  migrations/
  seed.sql
```

Use Supabase migrations for database schema changes once the MVP data model is finalized.

